{
  "name": "personal-site",
  "version": "1.0.0",
  "engines": {
    "node": "12.16.x"
  },
  "scripts": {
    "heroku-postbuild": "cd client && npm install && npm run build",
    "start-dev": "cd client && npm run dev",
    "start-server": "cd client && npm run server"
  },
  "dependencies": {
    "three": "^0.116.1"
  }
}
