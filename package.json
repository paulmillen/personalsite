{
  "name": "personal-site",
  "version": "1.0.0",
  "engines": {
    "node": "12.16.x"
  },
  "scripts": {
    "heroku-postbuild": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev",
    "server": "cd client && npm run server"
  }
}
